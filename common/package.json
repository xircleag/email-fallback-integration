{
  "name": "common",
  "version": "1.0.0",
  "description": "Email fallback common",
  "scripts": {
    "test": "mocha --require should --bail test/*spec.js"
  },
  "dependencies": {
    "@sendgrid/mail": "^6.0.0",
    "mustache": "^2.3.0"
  },
  "devDependencies": {
    "mocha": "^5.0.1",
    "should": "^13.2.1"
  }
}
